#include "widget.h"

// Now ~Widget() is declared, so the client only needs the declaration; the real definition is in widget.cpp. Compiles, links, runs, exits 0 (no output).
int main() { Widget w; }
